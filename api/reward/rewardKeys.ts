export const REWARD_QUERY_KEYS = Object.freeze({
	REWARD: 'reward',
	GET: 'get',
})

export const rewardKeys = Object.freeze({
	reward: [REWARD_QUERY_KEYS.REWARD],
	get: [REWARD_QUERY_KEYS.REWARD, REWARD_QUERY_KEYS.GET],
})
